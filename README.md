pkg update && upgrade -y

pkg install python

pkg install git -y

pip install requests

pip install bs4

pip install rich

pip install mechanize

rm -rf active-cookies

git clone https://github.com/DoD-99/active-cookies.git

ls

cd active-cookies

ls

python cookies.py
